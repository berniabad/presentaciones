# Source PowerPoint files — exceed GitHub's 100 MB limit, kept locally only
assets/ppts/
# PPTs de referencia (Maxus, etc.), pesados: se guardan local, no versionar
*.pptx

# Media extraído de los PPT (referencia local, no versionar)
assets/copa-marea/_media/
assets/copa-marea/*.pptx
assets/copa-marea/*.pdf

# Belada — imágenes intermedias de extracción del PDF (no versionar, ~154MB)
presentaciones/_viejas/belada-padel-presentacion/assets/extracted-pdf/

# McDonald's — screenshots de QA (artefactos de dev, ~30MB)
presentaciones/previate-mcdonald/qa/

# Reporte Kaiyi evento — pendiente de revisión, NO publicar todavía (se sirve estático si se commitea)
presentaciones/reporte-kaiyi-evento/

# Netflix The Hawk — originales crudos de las fotos (material fuente, ~47MB)
assets/netflix-the-hawk/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*~

# Logs & temp
*.log
.tmp/
tmp/

# Node (in case any tooling is added later)
node_modules/
